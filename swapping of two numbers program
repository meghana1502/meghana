#include <stdio.h>

int main()
{
    int x, y;
    printf("Enter two numbers to swap: ");
    scanf("%d%d",&x,&y);
    printf("The values before swapping: %d and %d",x,y);
    x = x * y; y = x / y; x = x / y;
    printf("\nThe values after swapping: %d and %d",x,y);
}
